"""Reusable pitch-boundary detection pipeline library.

This package holds all core logic. `main.py` at the repo root is the thin
execution entry point that wires these pieces together and runs them.
"""
